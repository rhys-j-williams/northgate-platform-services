# Consumed by the mock cx CLI in platform-tooling/mock-scanners/bin.
project:
  name: northgate-platform-services
  team: /CxServer/Northgate/CSWT
scan:
  preset: Northgate-High-And-Medium
  incremental: false
  language: java
  source: .
  exclude:
    - node_modules
    - dist
    - coverage
    - target
    - "**/*.spec.ts"
thresholds:
  high: 0
  medium: 5
report:
  formats: [json, html]
  output: .cx-reports
