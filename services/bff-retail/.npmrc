# Internal registry in the bank (Artifactory virtual repo npm-virtual). Locally Verdaccio on 4873
# serves @meridian/* and proxies the rest. domain-fixtures is a file: dependency here because the
# Verdaccio publish step in estate-up.sh runs after npm install in CI. PLAT-1877.
registry=https://registry.npmjs.org/
save-exact=true
package-lock=true
