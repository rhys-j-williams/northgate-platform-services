apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "svc.name" . }}-env
  labels:
    {{- include "svc.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
