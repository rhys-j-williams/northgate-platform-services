spring:
  main:
    banner-mode: off
  kafka:
    bootstrap-servers: localhost:1
    producer:
      properties:
        max.block.ms: 200

logging:
  config: classpath:log4j2-test.xml

meridian:
  security:
    enabled: false
