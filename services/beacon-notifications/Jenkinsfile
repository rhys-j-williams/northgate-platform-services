#!/usr/bin/env groovy
// beacon-notifications. Owner payments-platform. Coverage threshold matches sonar-project.properties; if you lower one, lower both, and raise a PLAT ticket saying why.
@Library('meridian-pipeline@v3') _

meridianJavaPipeline(
    agentLabel:        'maven-jdk11-rhel8',
    appName:           'beacon-notifications',
    modulePath:        'platform-services/services/beacon-notifications',
    helmChart:         'platform-tooling/helm/beacon-notifications',
    coverageThreshold: 25,
    checkmarxThresholds: [high: 0, medium: 0]   // compliance critical, GIS-STD-014
)
