apiVersion: v2
name: bedrock-adapter
description: Local/legacy chart for bedrock-adapter. The deployable chart is platform-tooling/helm/bedrock-adapter; this one is kept for the kind cluster and the DeploymentConfig rollback path (TOOL-1102).
type: application
version: 0.3.0
appVersion: "3.14.2"
maintainers:
  - name: payments-platform
    email: payments-platform@example.com
annotations:
  meridian.bank/ticket-key: PLAT
  meridian.bank/compliance-critical: "true"
