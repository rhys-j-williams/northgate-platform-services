spring:
  main:
    banner-mode: off
  jms:
    listener:
      auto-startup: false

logging:
  config: classpath:log4j2-test.xml

meridian:
  security:
    enabled: false
  mq:
    host: localhost
    port: 1
    receive-timeout-ms: 50
  bedrock:
    mode: fixture
