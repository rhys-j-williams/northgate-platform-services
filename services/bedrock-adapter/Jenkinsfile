#!/usr/bin/env groovy
// bedrock-adapter. Owner payments-platform. Coverage threshold matches sonar-project.properties; if you lower one, lower both, and raise a PLAT ticket saying why.
@Library('meridian-pipeline@v3') _

meridianJavaPipeline(
    agentLabel:        'maven-jdk11-rhel8',
    appName:           'bedrock-adapter',
    modulePath:        'platform-services/services/bedrock-adapter',
    helmChart:         'platform-tooling/helm/bedrock-adapter',
    coverageThreshold: 20,
    checkmarxThresholds: [high: 0, medium: 0]   // compliance critical, GIS-STD-014
)
