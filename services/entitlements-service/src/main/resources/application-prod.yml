# Oracle 19c on Exadata, CSWTPRD service, ENTL schema. Credentials via the Vault agent sidecar.
# Commented out for the same reason as every other service: the ojdbc artefact is only in the
# internal Artifactory virtual repo. See README, Datasources. GIS-1180 applies.
#spring:
#  datasource:
#    url: jdbc:oracle:thin:@//exa-cswt-prod-scan.northgate.internal:1521/CSWTPRD_SVC
#    driver-class-name: oracle.jdbc.OracleDriver
#    username: ${VAULT_DB_USER}
#    password: ${VAULT_DB_PASSWORD}
#    hikari:
#      maximum-pool-size: 16
#  jpa:
#    properties:
#      hibernate.dialect: org.hibernate.dialect.OracleDialect
#  flyway:
#    schemas: ENTL
#  kafka:
#    bootstrap-servers: redpanda-a.northgate.internal:9092,redpanda-b.northgate.internal:9092
#    security:
#      protocol: SASL_SSL

northgate:
  security:
    jwks-uri: https://keystone.northgate.internal/.well-known/jwks.json
    issuer: https://keystone.northgate.internal
  approvals:
    ttl-hours: 48
