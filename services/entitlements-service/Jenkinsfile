#!/usr/bin/env groovy
// entitlements-service. Owner identity-platform. Coverage threshold matches sonar-project.properties; if you lower one, lower both, and raise a PLAT ticket saying why.
@Library('meridian-pipeline@v3') _

meridianJavaPipeline(
    agentLabel:        'maven-jdk17-rhel9',
    appName:           'entitlements-service',
    modulePath:        'platform-services/services/entitlements-service',
    helmChart:         'platform-tooling/helm/entitlements-service',
    coverageThreshold: 55

)
